apiVersion: v1
kind: Service
metadata:
  name: goose-web
  namespace: goose-app
spec:
  type: NodePort

  selector:
    app: goose-web

  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080